@model List<Ventas>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    
</div>

<div class="card">
  <div class="card-header">
    Lista de Ventas
  </div>
  <div class="card-body">
   <a asp-action="Ventas" asp-controller="Home" asp-route-id="0" class="btn btn-success btn-sm">Nueva Venta</a>
   <hr/>
   <table class="table">
       <thead>
           <tr>
               <th>Id</th>
               <th>Fecha</th>
               <th>Cliente</th>
               <th>Vendedor</th>
               <th>Producto</th>
               <th>Importe</th>
           </tr>
       </thead>
       <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.fecha</td>
                        <td>@item.cliente</td>
                        <td>@item.vendedor</td>
                        <td>@item.producto</td>
                        <td>@item.importe</td>
                        <td>
                            <a asp-action="Venta" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-primary btn-sm">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
   </table>
  </div>
</div>